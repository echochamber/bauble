                            ┌─────────────────────────────────────────────┐
                            │              Claude Code                    │
                            │  (hooks fire on lifecycle events)           │
                            └──────┬──────────────────────────────────────┘
                                   │ PreToolUse / PostToolUse / Stop / etc.
                                   ▼
                  ┌────────────────────────────────────┐
                  │         hooks/ (bash)              │
                  │                                    │
                  │  pane-tint.sh ◄── bauble.conf      │
                  │    └─ set pane bg color            │
                  │    └─ set tab bar style            │
                  │    └─ write bauble-state.json      │
                  │    └─ auto-return after approval   │
                  │                                    │
                  │  session-track.sh                  │
                  │    └─ write session-map.json       │
                  │                                    │
                  │  waiting-context.sh                │
                  │    └─ set @claude-waiting-tool     │
                  │                                    │
                  │  pane-cleanup.sh / pane-silence.sh │
                  └──────────┬─────────────────────────┘
                             │ writes
                             ▼
              ┌──────────────────────────────┐
              │       State Files             │
              │                              │
              │  bauble-state.json           │
              │    {pane → state, timestamp} │
              │                              │
              │  session-map.json            │
              │    {pane → name, metadata}   │
              └──────┬───────────────────────┘
                     │ read by
                     ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    scripts/ (12 popup scripts)                      │
│                                                                     │
│  ┌─ PICKERS (FilterableList) ─────────────────────────────────────┐ │
│  │  tmux-claude-picker   session browser (grouped by state)       │ │
│  │  tmux-worktree        git worktree selector                    │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌─ SCROLLBACK SCANNERS (FilterableList + split viewer) ──────────┐ │
│  │  tmux-diff     edit markers + git changes → delta/less split   │ │
│  │  tmux-files    file:// URLs → viewer split                     │ │
│  │  tmux-notes    ~/notes/*.md grouped by session → glow split    │ │
│  │  tmux-glow     .md paths in scrollback → glow split            │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌─ ACTIONS (ActionList) ─────────────────────────────────────────┐ │
│  │  tmux-approve-all   batch y/n/g/s per waiting pane             │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌─ FORMS (Input/TextArea) ───────────────────────────────────────┐ │
│  │  tmux-quick-capture   route to note / bead / linear            │ │
│  │  tmux-quick-note      title + body → ~/notes/quick/            │ │
│  │  tmux-session-rename  rename pane + tab + session-map.json     │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ┌─ STATIC ──────────────────────────────────────────────────────┐  │
│  │  tmux-cheatsheet      keybinding reference (less pager)       │  │
│  │  tmux-beads           dashboard (→ tmux-beads-render.py)      │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌─ INFRASTRUCTURE (no UI, stay bash) ───────────────────────────┐  │
│  │  tmux-agent-status    status bar widget (polled 5s)           │  │
│  │  tmux-claude-next     navigate to next waiting pane           │  │
│  │  tmux-claude-breadcrumb  breadcrumb trail navigation          │  │
│  │  tmux-claim-bead      claim bead + rename window              │  │
│  │  tmux-diff-extract    diff extraction helper                  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
       │              │                │               │
       │ gum          │ tmux           │ subprocess    │ file I/O
       ▼              ▼                ▼               ▼
  ┌────────┐   ┌───────────┐   ┌────────────┐   ┌──────────────┐
  │  gum   │   │   tmux    │   │  External  │   │  File System │
  │        │   │           │   │  Tools     │   │              │
  │ filter │   │ list-pane │   │            │   │ ~/notes/     │
  │ choose │   │ send-keys │   │ delta      │   │ bauble.conf  │
  │ input  │   │ split-win │   │ glow       │   │ state JSON   │
  │ write  │   │ select-*  │   │ bat        │   │ session-map  │
  │ style  │   │ capture   │   │ less       │   │              │
  │ spin   │   │ display-* │   │ git        │   │              │
  └────────┘   │ set-option│   │ bd (beads) │   │              │
               └───────────┘   │ linear-add │   │              │
                               └────────────┘   └──────────────┘

  ┌─────────────────────────────────────────────────────────────┐
  │                   tmux/ + config/                           │
  │                                                             │
  │  bauble.tmux.conf     keybindings (prefix+g namespace)      │
  │  hooks.json           Claude Code hook registration         │
  │  bauble.conf          all configurable values               │
  │                         dirs, limits, thresholds            │
  │                         NEW: state colors + tab styles      │
  └─────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────┐
  │               NEW: bauble/tui/ (Python Textual)             │
  │                                                             │
  │  app.py          BaubleApp — screen registry, theme         │
  │  config.py       parse bauble.conf (same layering as bash)  │
  │  tmux.py         tmux CLI wrappers (explicit targeting)     │
  │  state.py        read state JSON + compute elapsed time     │
  │  scrollback.py   pane capture + regex scanning              │
  │  attach.py       CLI entry: daemon socket → cold fallback   │
  │  daemon.py       pre-fork server (optional, <50ms startup)  │
  │                                                             │
  │  widgets/                                                   │
  │    filterable_list.py   browse-first, / to search           │
  │    action_list.py       per-item y/n/g/s actions            │
  │                                                             │
  │  screens/               one per popup script                │
  │    picker, approve, diff, files, notes, glow,               │
  │    worktree, capture, note, rename, cheatsheet, beads       │
  └─────────────────────────────────────────────────────────────┘
